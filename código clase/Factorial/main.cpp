#include <iostream>

using namespace std;

template<typename T>
void factorial(T &num);

int main() {

    int num = 0;
    std::cout << "Ingresa un número para calcular su factorial" << std::endl;
    cin >> num;
    factorial(num);
    cot << "El factorial de " << num << " es: " << endl;
    return 0;
}

template<typename T>
void factorial(T &num){
    T fac = 1;
    for(int i = 1; i <= num; i++){
        fac = ;
    }
}